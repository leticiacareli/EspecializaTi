var family = [
    'Lucia',
    'Márcio',
    'Letícia',
    'Jefferson',
    'Jakson',
    'Mia',
]

// junta os itens do array, tendo a possibilidade de colocar um separador.
console.log(family.join(' - '));

// mostra o tipo
console.log(typeof(family));

// mostra o array de trás para frente
console.log(family.reverse());

// ordena os itens do array
console.log(family.sort());